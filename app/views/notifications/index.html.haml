#main
  %h2= current_user.department.name
  -@stations.each do |station|
    =render(:partial=>"station_notifier",:locals=>{:station=>station})
  =button_to "Add Station",new_user_subscription_path(current_user),{:method=>"get"}