%h1 Listing users
%table
  %tr
    %th Name
  -@users.each do |user| 
    %tr
      %t=h user.name 
      %t= link_to 'Edit', edit_user_path(user)
      %t= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete